This fixture keeps the label itself structurally matched by explicit id so the failure stays focused on target semantics.

Because both candidate inputs still exist in the same form, the analyzer should report direct target-resolution drift instead of unmatched-node fallout or a broader target-sanity problem.
